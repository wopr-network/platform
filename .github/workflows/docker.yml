name: Docker Build & Push

on:
  push:
    branches: [main]

env:
  REGISTRY: ghcr.io/wopr-network

jobs:
  detect-changes:
    runs-on: self-hosted
    outputs:
      matrix: ${{ steps.detect.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect changed products
        id: detect
        run: |
          CHANGED_FILES=$(git diff --name-only HEAD~1 HEAD)
          ITEMS=()

          # Core changes affect ALL products
          if echo "$CHANGED_FILES" | grep -q "^core/"; then
            echo 'matrix={"product":["wopr-platform","paperclip-platform","nemoclaw-platform","holyship","wopr-platform-ui","paperclip-platform-ui","nemoclaw-platform-ui","holyship-platform-ui"]}' >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "$CHANGED_FILES" | grep -q "^platforms/wopr-platform/" && ITEMS+=("wopr-platform")
          echo "$CHANGED_FILES" | grep -q "^platforms/paperclip-platform/" && ITEMS+=("paperclip-platform")
          echo "$CHANGED_FILES" | grep -q "^platforms/nemoclaw-platform/" && ITEMS+=("nemoclaw-platform")
          echo "$CHANGED_FILES" | grep -q "^platforms/holyship/" && ITEMS+=("holyship")
          echo "$CHANGED_FILES" | grep -q "^shells/wopr-platform-ui/" && ITEMS+=("wopr-platform-ui")
          echo "$CHANGED_FILES" | grep -q "^shells/paperclip-platform-ui/" && ITEMS+=("paperclip-platform-ui")
          echo "$CHANGED_FILES" | grep -q "^shells/nemoclaw-platform-ui/" && ITEMS+=("nemoclaw-platform-ui")
          echo "$CHANGED_FILES" | grep -q "^shells/holyship-platform-ui/" && ITEMS+=("holyship-platform-ui")
          echo "$CHANGED_FILES" | grep -q "^services/platform-crypto-server/" && ITEMS+=("platform-crypto-server")
          # Service changes affect their consumers
          if echo "$CHANGED_FILES" | grep -q "^services/provision-client/"; then
            ITEMS+=("wopr-platform" "paperclip-platform" "nemoclaw-platform")
          fi

          # Deduplicate
          UNIQUE=($(printf '%s\n' "${ITEMS[@]}" | sort -u))
          if [ ${#UNIQUE[@]} -eq 0 ]; then
            echo 'matrix={"product":[]}' >> "$GITHUB_OUTPUT"
          else
            PRODUCTS=$(printf '%s\n' "${UNIQUE[@]}" | jq -R . | jq -s -c .)
            echo "matrix={\"product\":$PRODUCTS}" >> "$GITHUB_OUTPUT"
          fi

  build-push:
    needs: detect-changes
    if: ${{ fromJson(needs.detect-changes.outputs.matrix).product[0] != null }}
    runs-on: self-hosted
    permissions:
      packages: write
    strategy:
      matrix: ${{ fromJson(needs.detect-changes.outputs.matrix) }}
    steps:
      - uses: actions/checkout@v4

      - name: Resolve Dockerfile path
        id: paths
        run: |
          case "${{ matrix.product }}" in
            wopr-platform)         echo "dockerfile=platforms/wopr-platform/Dockerfile" >> "$GITHUB_OUTPUT" ;;
            paperclip-platform)    echo "dockerfile=platforms/paperclip-platform/Dockerfile" >> "$GITHUB_OUTPUT" ;;
            nemoclaw-platform)     echo "dockerfile=platforms/nemoclaw-platform/Dockerfile" >> "$GITHUB_OUTPUT" ;;
            holyship)              echo "dockerfile=platforms/holyship/Dockerfile" >> "$GITHUB_OUTPUT" ;;
            wopr-platform-ui)      echo "dockerfile=shells/wopr-platform-ui/Dockerfile" >> "$GITHUB_OUTPUT" ;;
            paperclip-platform-ui) echo "dockerfile=shells/paperclip-platform-ui/Dockerfile" >> "$GITHUB_OUTPUT" ;;
            nemoclaw-platform-ui)  echo "dockerfile=shells/nemoclaw-platform-ui/Dockerfile" >> "$GITHUB_OUTPUT" ;;
            holyship-platform-ui)  echo "dockerfile=shells/holyship-platform-ui/Dockerfile" >> "$GITHUB_OUTPUT" ;;
            platform-crypto-server) echo "dockerfile=services/platform-crypto-server/Dockerfile" >> "$GITHUB_OUTPUT" ;;
          esac

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ steps.paths.outputs.dockerfile }}
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ matrix.product }}:staging
            ${{ env.REGISTRY }}/${{ matrix.product }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
